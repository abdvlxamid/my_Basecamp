<div id="<%= dom_id message %>" class="comment-form">
  <div class="card mb-3">
    <div class="card-header bg-info text-white">
      <%= message.user.email %>
    </div>
    <div class="card-body">
      <p class="card-text"><%= message.message %></p>
    </div>
  </div>
</div>
